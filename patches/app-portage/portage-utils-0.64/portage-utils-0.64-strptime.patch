--- portage-utils-0.64/qlop.c
+++ portage-utils-0.64/qlop.c
@@ -683,7 +683,7 @@ bool parse_date(const char *sdate, time_t *t)
 			if (strchr(sdate, '-') == NULL)
 				fmt = "%s";
 			else
-				fmt = "%F";
+				fmt = "%Y-%m-%d";
 
 			s = strptime(sdate, fmt, &tm);
 			if (s == NULL || s[0] != '\0')
