diff --git a/gcc/config/rs6000/rs6000.c b/gcc/config/rs6000/rs6000.c
index b0d5a22e68c1..8224498fad5a 100644
--- a/gcc/config/rs6000/rs6000.c
+++ b/gcc/config/rs6000/rs6000.c
@@ -4615,7 +4615,7 @@ rs6000_option_override_internal (bool global_init_p)
   /* Use long double size to select the appropriate long double.  We use
      TYPE_PRECISION to differentiate the 3 different long double types.  We map
      128 into the precision used for TFmode.  */
-  int default_long_double_size = (RS6000_DEFAULT_LONG_DOUBLE_SIZE == 64
+  int default_long_double_size = (RS6000_DEFAULT_LONG_DOUBLE_SIZE == 64 || !TARGET_64BIT
 				  ? 64
 				  : FLOAT_PRECISION_TFmode);
 
