diff --git firefox-60.4.0/build/moz.configure/rust.configure
index 51bd1c5..fc77021 100644
--- firefox-60.4.0/build/moz.configure/rust.configure.orig
+++ firefox-60.4.0/build/moz.configure/rust.configure
@@ -119,6 +119,8 @@ def rust_supported_targets(rustc):
     ambiguous = set()
     per_raw_os = {}
     for t in out:
+        if 'gentoo' not in t:
+            continue
         t = split_triplet(t, allow_unknown=True)
         key = (t.cpu, t.endianness, t.os)
         if key in per_os:
